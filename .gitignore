/.quarto/
**/*.quarto_ipynb
.playwright-mcp/
MGMT803.csv
Screenshot 2026-09-18*.png

# Attendance and participation scored after each class, a row per student per
# meeting. Student records -- never publish.
participation.csv

.DS_Store

# Voiceover render outputs -- the narrated MP4 and its intermediates are large
# binaries rebuilt from the deck, so only the source .qmd and the narration
# script are tracked.
video/.voiceover/
video/*.mp4
video/*.pdf
video/*.html
video/803_video_files/
